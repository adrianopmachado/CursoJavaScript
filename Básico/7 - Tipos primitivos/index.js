// string, number, undefined, null, boolean, symbol
const nome = 'Adriano'; // String
const num = 7 ;// number
const num2 = 10.8; // number
let nomeAluno; // undefined = Não aponta pra local nenhum na memória
let sobrenomeAluno = null; // Nulo = Não aponta pra local nenhum na memória
const verdadeiro = true; // Boolean = Verdadeiro ou falso (true ou false)
const falso = false; // Boolean = Verdadeiro ou falso (true ou false) = (lógico)

console.log(typeof nome);

